// #include "Globals.h"

// // Define the constants here — exactly once
// const int W = 1200;
// const int H = 800;
// const float DEGTORAD = 0.017453f;

#include "Globals.h"

const int W = 1200;
const int H = 800;
const float DEGTORAD = 0.017453f;

//This ensures the constants exist in one place in memory, while the header just lets other files know about them using extern if needed.